services:
  - type: web
    name: trademind-waitlist
    env: node
    buildCommand: npm install && npm run build
    startCommand: next start -p $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    autoDeploy: true
